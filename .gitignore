node_modules/
out/
dist/
.DS_Store
*.log

# Build output (large, machine-specific)
release/
Latency-*-win/
*.tsbuildinfo

# iOS / Android / Capacitor build artifacts — the native projects are
# regenerated in CI (cap add ios|android) on every run, so they're never committed.
mobile/ios/
mobile/android/
mobile/dist/
# generated in CI from assets/appicon-source.png for @capacitor/assets
mobile/assets/icon.png
Payload/
*.ipa
*.apk
DerivedData/
Pods/
.gradle/

# Local/runtime secrets — SoundCloud token lives in userData, never commit a copy
soundcloud.json
.env
.env.*
