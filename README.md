# Latency

Десктопный медиаплеер уровня Spotify, написанный с нуля на **Electron + Vite +
React + TypeScript**. В основе — архитектура подключаемых источников
воспроизведения (`PlaybackProvider`): локальные файлы, SoundCloud и **Яндекс
Музыка** работают через единое ядро, новый источник добавляется реализацией
одного интерфейса.

Собственный UI (тёмная тема, неоновый акцент, кастомная рамка окна), реактивный
визуализатор, караоке-тексты (LRCLIB + Genius), персональные миксы и гибкое
оформление. Кроссплатформенно: десктоп (Windows, Linux) + мобильные сборки
(iOS, Android) через Capacitor — на телефоне тот же интерфейс и те же экраны.

Написан чистым вайбкодингом — [icountedtheblink](https://github.com/nxrtwee) и Claude.

## Загрузки

Готовые сборки — на вкладке **[Releases](../../releases/latest)**:

| Платформа | Версия | Файл | Установка |
|-----------|--------|------|-----------|
| **Windows** | 1.6.1 | `Latency-1.6.1-x64.exe` (установщик) или `-portable.exe` | запустить установщик |
| **Linux** | 1.6.1 | `Latency-1.6.1-x86_64.AppImage` (универсальный) или `Latency-1.6.1-x86_64.rpm` (Fedora/RHEL) | AppImage — `chmod +x` и запустить; rpm — `sudo dnf install` |
| **Android** | 1.6.1 | `Latency-1.6.1.apk` | включить «установка из неизвестных источников» → открыть APK |
| **iOS** | 1.6.1 | `Latency-1.6.1-unsigned.ipa` | подписать своим Apple ID через [AltStore](https://altstore.io/) / [Sideloadly](https://sideloadly.io/) (бесплатный аккаунт — перевыпуск раз в 7 дней) |

> 🖥️ **Все платформы под единой версией.** Текущая версия — **1.6.1**.
> Мобильные сборки (Android/iOS) рисуют **сам десктопный интерфейс** (скин
> nextgen, раскладка Universal) в нативной обвязке — нижние табы, сайдбар в
> ящике, капсула плеера над табами. Экраны, механики и настройки те же, что на
> десктопе; телефонного там только обвязка и «Масштаб интерфейса». Из десктопного
> не приехало лишь то, чего на телефоне нет по устройству системы: эквалайзер
> (стрим играет вне WebView), Discord Rich Presence, глобальные горячие клавиши и
> автозапуск. Планшетная раскладка ещё впереди — на iPad пока работает
> телефонная.

## Возможности

- **Локальные файлы** — скан папок, чтение тегов и обложек (`music-metadata`),
  отдача через собственный протокол `media://`.
- **SoundCloud** — поиск треков и артистов, стрим через публичный API:
  progressive (MP3) и **HLS** (через `hls.js`); `client_id` подбирается
  автоматически; вход в аккаунт → персональные миксы и лайки.
- **Яндекс Музыка** — работает **без VPN в России** (SoundCloud в РФ заблокирован).
  Вход своим аккаунтом (OAuth, токен локально), стрим по вашей подписке Яндекс
  Плюс; поиск треков/артистов, импорт лайков. Без обхода блокировок и DRM.
- **Моя волна** — персональное бесконечное радио Яндекса с самопополняющейся
  очередью; рандомизируется при каждом запуске.
- **Радио по треку и артисту** — «Моя волна по треку» / «по артисту» (ротор
  Яндекса) из меню трека и со страницы артиста; для SoundCloud — станция-автопилот
  от выбранного трека.
- **Выравнивание громкости** — приводит треки к единой громкости по метаданным
  (R128/LUFS у Яндекса, ReplayGain у локальных), с защитой от клиппинга по пику.
- **Кроссфейд** — плавные переходы между треками, 0–12 с (Настройки → Звук).
- **Альбомы и плейлисты** — дискография на странице артиста, страницы альбомов и
  плейлистов, поиск альбомов/плейлистов (ЯМ и SC) с переключателями в настройках.
- **Страница артиста** — слушатели/подписчики, прослушивания на треках,
  похожие артисты.
- **Системный медиаплеер (Windows)** — обложка, название, исполнитель и прогресс
  в системном медиа-оверлее; кнопки play/pause/next/prev и медиа-клавиши.
- **Поиск по строчке** — помните фрагмент песни? Вкладка «Поиск» →
  режим «По строчке»: Genius → находим трек на SoundCloud или в Яндекс Музыке.
- **10-полосный эквалайзер** — Web Audio BiquadFilter, 7 пресетов (Flat, Bass,
  Vocal, Electronic…), локальные файлы и SoundCloud.
- **Discord Rich Presence** — название трека и обложка в профиле Discord.
- **Комментарии SoundCloud** — вкладка с обложкой, визуализатором и всплывающими
  комментариями по таймкодам (как на оригинальном сайте SC).
- **Автопилот** — когда очередь кончается, плеер сам находит похожие треки и
  продолжает играть без остановки.
- **Офлайн-кэш** — скачанные треки SoundCloud и Яндекс Музыки играют без интернета,
  вкладка «Скачанное» в сайдбаре.
- **Единое ядро** — очередь, плеер-бар, лайки, плейлисты не зависят от источника.
- **Библиотека** — Home, поиск, артисты, плейлисты, «Любимое», недавнее.
- **Караоке-тексты** — LRCLIB (синхронные) + Genius (фолбэк), ручная синхронизация.
- **Два скина интерфейса** — классический **oldgen** и кинематографичный
  **nextgen** (ambient-фон, грейн, стеклянные панели, cover-forward карточки,
  выразительные анимации); переключение в настройках, по умолчанию nextgen.
- **Визуализатор**, кастомные обои/акценты, караоке в полноэкранном плеере.
- **Кадрирование фона** — картинку или MP4 в качестве фона интерфейса можно
  двигать и зумить (на телефоне — пальцем, с пинч-зумом); перетаскивание точное,
  один пиксель пальца = один пиксель картинки.
- **Синхронизация лайков между устройствами** — выгрузка любимых треков
  (SoundCloud + Яндекс) вместе с их порядком в JSON и загрузка на другом
  устройстве, в обе стороны: десктоп ↔ телефон. Сервера нет, переносится файл.
- **Кастомный фон вкладок** — своя картинка в шапке «Любимого», «Недавнего»,
  «Скачанного», локальной библиотеки и плейлистов.
- **Масштаб интерфейса (телефон)** — ползунок 80–130 % в настройках: размер
  интерфейса на телефоне задаёт плотность экрана, а не его формат, поэтому на
  720p-панели всё выглядит крупным, а на флагманском 1440p — мелким.
- **Анимация смены трека** в плеер-баре и правой панели (простая на oldgen,
  выразительная на nextgen; пружинная на мобайле).
- Drag-and-drop и фильтр в панели очереди.
- Два языка: English / Русский.

## Разработка

```bash
npm install
npm run dev        # десктоп, дев-режим с hot-reload
npm run dev:mobile # мобильный таргет в браузере (http://127.0.0.1:5273)
```

> Если `npm run dev` падает с `Error: Electron uninstall` — бинарник Electron не
> распаковался при установке: `node node_modules/electron/install.js`.

## Сборка

```bash
npm run build      # main + preload + renderer в out/
npm run typecheck  # проверка типов (node + web)
npm run dist:win   # установщик (NSIS) + портативный exe в release/
```

> Для `dist:win` локально на Windows нужен включённый **Developer Mode** (иначе
> electron-builder падает на распаковке winCodeSign с `Cannot create symbolic
> link`). В CI собирается автоматически — см. ниже.

### Релизы (CI)

Все три артефакта собираются в облаке GitHub Actions и публикуются в Release при
пуше тега `v*`:

- **Windows** — `windows-latest`, electron-builder (NSIS + portable);
- **Android** — `ubuntu-latest`, Capacitor + Gradle (debug-APK, ставится сразу);
- **iOS** — `macos`, Capacitor + xcodebuild (неподписанный `.ipa`, подпись при
  сайдлоаде).

Мобильная обвязка (нативные правки, фоновое аудио, иконки) описана в
[`mobile/README.md`](mobile/README.md), [`mobile/android-notes.md`](mobile/android-notes.md)
и [`mobile/ios-notes.md`](mobile/ios-notes.md).

## Архитектура

```
src/
  shared/types.ts          — общие типы (Track, LibraryState, Playlist)
  main/
    index.ts               — окно, IPC, протокол media:// для локальных файлов
    library.ts             — скан папок, теги и кэш обложек (music-metadata)
    soundcloud.ts          — client_id, поиск, резолв стрима (progressive/HLS)
    yandex.ts              — Яндекс Музыка: OAuth, поиск, стрим, «Моя волна» (ротор)
    lyrics.ts              — тексты (LRCLIB + Genius) + поиск по строчке
    discord.ts             — Discord Rich Presence (IPC-протокол)
    offline.ts             — офлайн-кэш треков SoundCloud и Яндекс Музыки
    likes.ts / playlists.ts — персист в userData
  preload/index.ts         — безопасный мост window.api (contextBridge)
  renderer/src/
    providers/             — PlaybackProvider: local, soundcloud, registry
    store.ts               — zustand-стор: источники, лайки, плейлисты, плеер
    components/             — Sidebar, TrackList, PlayerBar, CommentsPage, Equalizer, …
mobile/                     — Capacitor-таргет (iOS + Android), реюз renderer
```

### Как добавить новый источник

1. Реализовать `PlaybackProvider` в `src/renderer/src/providers/<источник>.ts`.
2. Зарегистрировать его в `registry.ts`.
3. Отдавать треки с нужным `providerId` и `uri`.

Ядро (стор, UI, очередь) не меняется — оно работает с любым провайдером.

## Границы по легальности

- **Локальные файлы** — без ограничений.
- **SoundCloud** — через публичный API/стрим-URL и личный OAuth-токен самого
  пользователя (хранится только локально, в репозиторий не попадает).
- Обход DRM и риппинг защищённого аудио (Spotify/Яндекс) в проекте не делается.

## Лицензия

MIT
