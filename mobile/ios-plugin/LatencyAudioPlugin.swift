// LatencyAudioPlugin.swift — Capacitor plugin registration for LatencyAudio.
// This file makes the plugin discoverable by `cap sync`.

import Capacitor

@objc(LatencyAudioPlugin)
public class LatencyAudioPlugin: CAPPlugin {
    // The plugin class is registered via @objc annotation;
    // Capacitor finds it by name "LatencyAudio" from the bridged protocol.
}
