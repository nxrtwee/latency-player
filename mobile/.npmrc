# @jofr/capacitor-media-session declares a peerDep of @capacitor/core ^6, but its
# native MediaSession code reads the shared Gradle variables and builds fine under
# Capacitor 8. Allow the peer mismatch so npm install / npm ci don't error out.
legacy-peer-deps=true
